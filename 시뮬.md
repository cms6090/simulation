# 시뮬

태그: 연구
상태: 시작 전

> **핵심 목표**
> 
> 
> 설명변수 $X_1,X_2$에서 MC와 Split CP의 coverage·구간 중심·반폭·전체 길이를 비교하고, 그 차이가 구조·fitting·calibration 중 어디에서 발생하는지 확인
> 
> **Coverage·구간 중심·폭 차이 계산 → 각 차이를 Structural·Fitting·Calibration으로 분해 → 세 평가 관점에서 원인 해석**
> 
> 오차 분해는 필수 핵심 분석이며, heatmap은 계산 결과를 보여주는 후속 시각화
> 

---

## 연구 구조의 네 구분

| 구분 | 내용 |
| --- | --- |
| **평가지표** | Marginal·conditional coverage, 구간 중심·반폭·전체 길이 |
| **차이 분해** | 각 지표에서 Structural → Fitting → Calibration |
| **평가 관점** | Marginal / fixed-$x$ / fixed-calibration |
| **결과 표현** | 표, 필요 시 grid 기반 heatmap |

분해 대상은 지표 자체가 아니라 **그 지표에서 나타나는 구간 간 차이**

---

# 1. 전체 실험 구조

**두 설명변수의 DGP 설정**

→ training data로 OLS·RF 학습

→ 독립 calibration으로 실제 Split CP 구간 구성

→ Oracle·MC·true-residual·fitted-population 구간도 구성

→ 같은 위치와 같은 반복에서 다섯 구간의 **coverage·중심·폭 계산**

→ **CP − MC의 전체 차이 계산**

→ **Structural·Fitting·Calibration 차이와 MC 근사 차이 계산**

→ Marginal·fixed-$x$·fixed-calibration 관점에서 각 항의 평균·변동·위치별 패턴 확인

→ 이후 표·heatmap으로 결과 제시

> **평가지표 → 차이의 3단계 분해 → 세 평가 관점 → 결과 표현**의 순서로 설계
> 
> 
> Grid와 heatmap은 마지막 결과 표현 단계이며, 평가지표나 분해 정의에 필수인 것은 아님
> 

---

# 2. 기본 DGP와 실험 조건

## 2.1 설명변수와 반응변수

$$
X=(X_1,X_2),\qquad X_1,X_2\overset{iid}{\sim}U(-1,1),\qquad X\perp\!\!\!\perp\varepsilon
$$

$$
Y=m_0(X_1,X_2)+\sigma_0(X_1,X_2)\varepsilon
$$

- $x=(x_1,x_2)$: 입력 공간의 한 위치
- $m_0(x)$: true conditional mean
- $\sigma_0(x)>0$: true conditional scale
- $E[\varepsilon]=0$, $\operatorname{Var}(\varepsilon)=1$
- training·calibration·평가 데이터는 같은 DGP에서 서로 독립 생성

## 2.2 Mean function — 2차원 설계안

| 구분 | 함수 | 목적 |
| --- | --- | --- |
| Linear | $m_0(x)=\sqrt2(x_1+x_2)$ | 두 변수가 관여하며 선형 OLS가 올바르게 지정된 조건 |
| Nonlinear | 아래 식 | 비선형 주효과와 두 변수의 상호작용 |

$$
m_0^{nonlin}(x_1,x_2)
=\sqrt{\frac6{11}}\left\{2\sin(\pi x_1)+x_2+x_1x_2\right\}
$$

- $2\sin(\pi x_1)$: $X_1$의 비선형 효과
- $x_2$: $X_2$의 선형 효과
- $x_1x_2$: 두 변수의 상호작용
- OLS에는 원변수 두 개와 절편만 입력하므로 nonlinear 조건에서는 mean misspecification 존재

**신호 크기 조정:** 두 mean 모두 $E_X[m_0(X)]=0$, $\operatorname{Var}_X\{m_0(X)\}=4/3$

Nonlinear 중괄호 안의 분산은 $2+1/3+1/9=22/9$이므로 $\sqrt{6/11}$을 곱하여 분산 $4/3$으로 조정

> Linear·nonlinear 비교에서 신호 분산 차이를 줄이기 위한 설정이며, 두 함수의 학습 난도가 같다는 뜻은 아님
> 

## 2.3 Scale function

| 구분 | $\sigma_0(x_1,x_2)$ | 목적 |
| --- | --- | --- |
| Homoskedastic | $1$ | 모든 위치에서 동일한 noise scale |
| Heteroskedastic | $\displaystyle \frac{0.4+1.2\sin^2(\pi x_1)}{\sqrt{1.18}}$ | 평균 noise variance는 1로 유지하면서 $x_1$에 따른 noise scale 변화 |

기존 scale의 형태를 유지하며, mean은 두 변수에 의존하되 scale은 $X_1$에만 의존

이분산 조건에서는 원래 scale 함수 $0.4+1.2\sin^2(\pi x_1)$의 평균 제곱값이 $1.18$이므로 $\sqrt{1.18}$로 나누어 표준화

$$
E_X\left[\left\{0.4+1.2\sin^2(\pi X_1)\right\}^2\right]=1.18
$$

따라서

$$
E_X\left[\sigma_0^2(X)\right]
=
E_X\left[
\left\{
\frac{0.4+1.2\sin^2(\pi X_1)}{\sqrt{1.18}}
\right\}^2
\right]
=1
$$

$\operatorname{Var}(\varepsilon)=1$이므로 두 scale 조건 모두 평균 conditional noise variance는 동일

$$
E_X[\operatorname{Var}(Y\mid X)]
=
E_X[\sigma_0^2(X)]
=1
$$

> **Homoskedastic·heteroskedastic 조건의 평균 noise variance를 동일하게 맞추고, 위치에 따른 noise scale 변화 자체의 영향을 비교**
> 
> 
> $E[\sigma_0(X)]=1$을 맞추는 것이 아니라 conditional variance가 $\sigma_0^2(X)$이므로 $E[\sigma_0^2(X)]=1$로 표준화
> 

정규화 전 scale 범위는 $[0.4,1.6]$, 정규화 후에는 대략 $[0.368,1.472]$이며 상대적인 이분산 패턴은 그대로 유지

## 2.4 Error distribution

| 구분 | 정의 | 특징 |
| --- | --- | --- |
| Gaussian | $\varepsilon\sim N(0,1)$ | 대칭 |
| Student-$t$ | $\varepsilon=T/\sqrt3$, $T\sim t_3$ | 대칭·heavy tail·분산 1 |
| LogNormal | 아래 표준화 | 비대칭·평균 0·분산 1 |

$Z\sim N(0,1)$을 사용한 LogNormal 설계안

$$
\varepsilon=\frac{e^Z-e^{1/2}}{\sqrt{e(e-1)}}
$$

원래 문서에 로그분포 모수가 없었으므로, 기존 코드가 있다면 위 모수와 일치 여부 확인 필요

## 2.5 모델과 반복 설정

| 항목 | 설정 |
| --- | --- |
| Model | OLS, RF |
| OLS | 절편 + $X_1,X_2$, nonlinear·interaction basis 추가 없음 |
| RF | 회귀 forest, 1,000 trees |
| 목표 coverage | 90%, 95%, 99% |
| $n_{train}=n$ | 1000 |
| $n_{cal}=m$ | 1000 |
| $B=R_{sim}$ | 독립 training·기본 calibration 반복 200회 |
| Fixed-$x$ training ID | 각 DGP의 $b=1,\ldots,10$, 결과와 무관하게 사전 지정 |
| $R_{cal}$ | 위 각 fitted model에서 calibration 총 500회 |
| $M_{MC}$ | MC 구간 생성 표본 수, pilot에서 결정 |
| $M_{pop}$ | population quantile 근사용 입력 수, pilot에서 결정 |
| $N_{eval}$ | marginal 평가용 독립 입력 수, pilot에서 결정 |

총 **2 mean × 2 scale × 3 error = 12개 DGP**, OLS·RF 포함 **24개 model–DGP 조합**

90%·95%·99%까지 구분하면 72개 결과 조건이며, coverage별 모델 재학습은 불필요

RF의 leaf size·depth·feature sampling·bootstrap·구현체·버전은 실행 전 명시하고 동일 규칙 적용

OLS·RF는 같은 training·calibration을 공유하며, calibration을 튜닝에 사용하지 않음

---

# 3. 반복과 기호

| 기호 | 의미 |
| --- | --- |
| $j\in\{\mathrm{OLS},\mathrm{RF}\}$ | 모델 종류 |
| $b=1,\ldots,B$ | training 반복 |
| $r=1,\ldots,R_{cal}$ | 한 fitted model에서의 calibration 반복 |
| $i=1,\ldots,m$ | calibration 관측치 |
| $D_{train}^{(b)}$ | training data |
| $\widehat m_j^{(b)}$ | 해당 training에서 학습한 fitted model |
| $D_{cal}^{(b,r)}$ | 해당 training과 독립인 calibration data |
| $C_{CP,j}^{(b,r)}(x)$ | 해당 반복에서 만든 CP 구간 |

기본 분석은 각 $b$마다 calibration 하나, 즉 $r=1$ 사용

**Fixed-$x$ 반복의 구현 기본값:** 각 DGP의 training ID $b=1,\ldots,10$에 대해 OLS·RF 각각 calibration 총 $R_{cal}=500$회 평가

- ID 10개는 이번 보완에서 정한 실행 기본값이며 교수님이 지정한 수치나 충분성을 보장하는 수치는 아님
- 기본 분석의 $r=1$ calibration을 재사용하고 $r=2,\ldots,500$을 추가 생성
- 같은 $(b,r)$에서는 OLS·RF가 calibration data 공유
- 각 $b$의 결과를 따로 보고하며 10개 모델을 한 모델처럼 합치지 않음
- 기본 $B=200$회 결과로 training·calibration 평균의 위치별 지표도 별도 계산
- 결과가 좋거나 나쁜 training을 사후 선택하여 ID를 바꾸지 않음

## 세 관점에서 공통으로 지키는 구간 생성 규칙

세 관점은 서로 다른 CP 알고리즘이 아니라 **같은 구간 생성법의 평가 관점**

Training으로 fitted model 학습

→ 어느 관점이든 calibration $(X_i,Y_i)$를 원래 전체 DGP에서 iid 생성

→ Absolute residual과 같은 conformal 순위 규칙으로 구간 구성

→ 평가할 때 $x$ 또는 calibration을 고정하거나 평균

**Fixed-$x$에서도 calibration의 $X_i$를 전부 $x_0$로 두지 않음** — 고정하는 것은 평가 위치이며, calibration은 계속 전체 입력분포에서 생성

$C_{res,0}$와 $C_{pop,j}$의 population residual 분위수도 전체 $X$ 분포 기준으로 유지

Fixed-calibration에서는 calibration과 fitted model을 함께 고정한 상태로 $x$를 변화시키며, 구간을 평가하는 위치마다 재학습·재calibration하지 않음

아래 구간 정의에서는 가독성을 위해 $b,r$ 첨자 일부 생략

---

# 4. 다섯 구간을 만드는 과정

## 4.1 한눈에 비교

| 구간 | 사용하는 중심·분포 | 분위수 | 역할 |
| --- | --- | --- | --- |
| $C_{Oracle}$ | true $Y\mid X=x$ | 정확한 양쪽 분위수 | 이론적 기준 |
| $C_{MC}$ | true DGP에서 생성한 $Y\mid X=x$ | 표본의 양쪽 분위수 | 실제 headline benchmark |
| $C_{res,0}$ | true mean 중심 | true absolute residual의 population quantile | 구조 차이 확인 |
| $C_{pop,j}$ | fitted model 중심 | fitted absolute residual의 population quantile | fitting 차이 확인 |
| $C_{CP,j}$ | fitted model 중심 | finite calibration의 conformal quantile | 실제 Split CP |

## 4.2 $C_{Oracle}$ — 정확한 conditional equal-tailed PI

**true model, true sigma, true error를 알기 때문에 $\epsilon$의 quantile을 수식적으로 계산해 만든 예측 구간**

특정 위치 $x=(x_1,x_2)$ 선택

→ true mean $m_0(x)$와 true scale $\sigma_0(x)$ 계산

→ true error distribution의 양쪽 분위수 $q_{\alpha/2}(\varepsilon)$, $q_{1-\alpha/2}(\varepsilon)$ 계산

→ 해당 위치의 conditional 구간 구성

$$
C_{Oracle}(x)=\left[
m_0(x)+\sigma_0(x)q_{\alpha/2}(\varepsilon),\;
m_0(x)+\sigma_0(x)q_{1-\alpha/2}(\varepsilon)
\right]
$$

> **True DGP를 알고 양쪽 이론 분위수로 계산한 구간**
> 

현재의 연속 오차분포에서는 모든 위치에서 conditional coverage $1-\alpha$

비대칭 오차에서는 구간 중심이 true mean과 다를 수 있으며, 이 구간이 항상 최단 길이인 것은 아님

## 4.3 $C_{MC}$ — true DGP 표본으로 계산한 구간

**true model, true sigma, true error를 알기 때문에 특정 x에서의 Y를 $M_{MC}$개수만큼 뽑아 해당 Y들을 순서대로 나열해 직접 quantile을 구해 만든 예측 구간**

특정 위치 $x=(x_1,x_2)$ 선택

→ true DGP에서 해당 위치의 반응변수 $M_{MC}$개 생성

$$
Y^{(s)}(x)=m_0(x)+\sigma_0(x)\varepsilon^{(s)},\qquad s=1,\ldots,M_{MC}
$$

→ 생성한 $Y$들의 아래·위 empirical quantile 계산

→ MC 구간 구성

$$
C_{MC}(x)=\left[
\widehat q_{\alpha/2}^{MC}(Y\mid X=x),\;
\widehat q_{1-\alpha/2}^{MC}(Y\mid X=x)
\right]
$$

> **True DGP에서 많이 생성한 표본의 분위수로 Oracle을 근사한 구간**
> 

| 목표 coverage | 아래 분위수 | 위 분위수 |
| --- | --- | --- |
| 90% | 5% | 95% |
| 95% | 2.5% | 97.5% |
| 99% | 0.5% | 99.5% |

계산 시 같은 error sample의 empirical quantile을 모든 평가 위치에 변환하여 재사용 가능

이 경우 위치 간 MC 오차는 상관되며, 독립 seed의 pilot에서 정확도 확인

True quantile은 MC 구간 자체를 만드는 데 대입하지 않고 Oracle 비교에 사용

## 4.4 $C_{res,0}$ — true mean + population residual 기준값

**CP 방식으로 score는 계산하는데, fitted model 대신 true model을 쓰고, finite calibration 대신 무한 calibration population을 쓴 것**

Fitted model 대신 true mean $m_0$ 사용

→ 특정 위치 하나가 아니라 **전체 $X$ 분포에서 나온 새로운 $(X,Y)$**를 생각

→ true mean에 대한 absolute residual 정의

$$
R_0=|Y-m_0(X)|=|\sigma_0(X)\varepsilon|
$$

→ 이 residual 분포의 정확한 $1-\alpha$ population quantile 계산

$$
q_{0,\alpha}=q_{1-\alpha}(R_0)
$$

→ 같은 분위수를 모든 위치에 적용하여 구간 구성

$$
C_{res,0}(x)=\left[m_0(x)-q_{0,\alpha},\;m_0(x)+q_{0,\alpha}\right]
$$

> **True mean을 사용하지만, 모든 위치의 residual을 합친 하나의 분위수로 폭을 정하는 구간**
> 

$X$에 대해 평균한 coverage는 현재 DGP에서 $1-\alpha$이나, 모든 위치에서 같은 coverage를 갖는 것은 아님

## 4.5 $C_{pop,j}$ — fitted model + population residual 기준값

**CP 방식으로 score는 계산, fitted model을 쓰고, finite calibration 대신 무한 calibration population을 쓴 것**

Training data로 모델 $\widehat m_j$ 학습

→ **Fitted model 고정**

→ 독립 calibration data가 무한히 많다고 가정

→ 새로운 관측치의 fitted absolute residual 정의

$$
R_j=|Y-\widehat m_j(X)|
$$

→ 고정된 fitted model에 대한 population residual quantile 계산

$$
q_{pop,j,\alpha}=q_{1-\alpha}\left(|Y-\widehat m_j(X)|\mid\widehat m_j\right)
$$

→ 구간 구성

$$
C_{pop,j}(x)=\left[\widehat m_j(x)-q_{pop,j,\alpha},\;\widehat m_j(x)+q_{pop,j,\alpha}\right]
$$

> **Fitted model은 그대로 두고, calibration이 무한히 많을 때의 분위수를 사용하는 이상적 residual 구간**
> 

Training residual 분위수가 아니며, training 반복마다 $q_{pop,j,\alpha}$ 재계산

## 4.6 $C_{CP,j}$ — 실제 finite-sample Split CP

**CP 방식으로 score는 계산, fitted model을 쓰고, finite calibration을 쓴 것**

Training data로 모델 $\widehat m_j$ 학습

→ 실제 **독립 calibration data $m$개**에서 residual 계산

$$
R_i=|Y_i-\widehat m_j(X_i)|,\qquad i=1,\ldots,m
$$

→ $m$개의 residual을 오름차순 정렬

→ conformal 순위 $k_\alpha=\lceil(m+1)(1-\alpha)\rceil$ 계산

→ 해당 순서통계량을 conformal quantile로 선택

$$
\widehat q_{j,\alpha}=
\begin{cases}
R_{(k_\alpha)},&k_\alpha\le m,\\
+\infty,&k_\alpha=m+1
\end{cases}
$$

→ 구간 구성

$$
C_{CP,j}(x)=\left[\widehat m_j(x)-\widehat q_{j,\alpha},\;\widehat m_j(x)+\widehat q_{j,\alpha}\right]
$$

> **Fitted model + finite calibration data로 실제 계산한 Split CP interval**
> 

| $m=1000$ | 90% | 95% | 99% |
| --- | --- | --- | --- |
| 선택 순위 | 901번째 | 951번째 | 991번째 |

정렬 배열의 `k - 1` 원소를 직접 사용하여 기본 보간 quantile과 혼동 방지

독립 calibration·test score가 연속인 현재 DGP에서, fitted model을 고정하고 calibration·새로운 $(X,Y)$를 평균한 coverage는 $k_\alpha/(m+1)$

이 보장은 **특정 위치 또는 특정 calibration set마다 목표 coverage를 만족한다는 보장이 아님** [1]

---

# 5. Population quantile의 실제 계산 과정

> ‘Calibration이 무한히 많다’는 정의상 가정이며 실제로 무한한 데이터를 생성하지 않음
> 
> 
> → finite calibration residual의 empirical quantile을 population residual distribution의 quantile로 대체
> 

## 5.1 공통 계산식

중심 함수 $g$를 고정($C_{res,0}$에선 $m_0(x)$, $C_{pop}$에서는 $\widehat m(x)$)

→ Residual score는 $R_g=|Y-g(X)|$

전체 입력에 대한 population CDF는 $H_g(q)=P(R_g\le q\mid g)$로 정의

먼저 특정 위치 $X=x$에서의 확률과 전체 $X$에 대한 평균을 구분

$$
\begin{aligned}
p_g(q,x)
&=P\{|Y-g(x)|\le q\mid X=x,g\}\\[4pt]
&=P\{-q\le m_0(x)+\sigma_0(x)\varepsilon-g(x)\le q\mid X=x,g\}\\[4pt]
&=P\left\{
\frac{g(x)-q-m_0(x)}{\sigma_0(x)}
\le\varepsilon\le
\frac{g(x)+q-m_0(x)}{\sigma_0(x)}
\,\middle|\,X=x,g\right\}
\end{aligned}
$$

→ 특정 $x$에서 반폭 $q$인 구간 $[g(x)-q,g(x)+q]$의 확률 계산

$$
p_g(q,x)=F_\varepsilon\!\left(\frac{g(x)+q-m_0(x)}{\sigma_0(x)}\right)
-F_\varepsilon\!\left(\frac{g(x)-q-m_0(x)}{\sigma_0(x)}\right)
$$

→ 전체 $X$ 분포에 대해 평균($X$도 랜덤이기 때문에 전체 $X$분포에 대해 평균해야 함)

$$
H_g(q)=E_X[p_g(q,X)]=P\{|Y-g(X)|\le q\mid g\}
$$

→ $H_g(q)=1-\alpha$를 만족하는 $q$ 탐색

| 목표 | 대입 |
| --- | --- |
| $q_{0,\alpha}$ | $g=m_0$ |
| $q_{pop,j,\alpha}$ | $g=\widehat m_j$ |

## 5.2 수치 계산 순서

<aside>
⚠️

**문제**
eg) $X$가 5차원이고 $X\sim U([-1,1]^5)$이면 위 기대값을 정확하게 계산하는 것은 어려움

</aside>

독립 reference 입력 $X^{ref}_1,\ldots,X^{ref}_{M_{pop}}\sim P_X$ 생성

→ 각 입력에서 $m_0$, $\sigma_0$, $\widehat m_j$ 계산

→ 후보 $q$에서 $p_g(q,X^{ref}_t)$ 계산

→ $\widetilde H_g(q)=\frac{1}{M_{pop}}\sum_{t=1}^{M_{pop}} p_g(q,X^{ref}_t)$ 계산($E_X[p_g(q,X)]$의 근사)

#### **이제 $q$는 어떻게 찾는가?**

우리가 원하는 건 $H_g(q)=1-\alpha$를 만족하는 $q$ 찾기 → 실제 계산에서는 $\widetilde H_g(q)=1-\alpha$를 만족하는 q 찾기

→ $q$가 커질수록 $[g(X)-q,g(X)+q]$가 넓어져 $\widetilde H_g(q)$도 증가

→ 이분법 등으로 $\widetilde H_g(q)=1-\alpha$의 해 탐색

→ $M_{pop}$ 확대와 독립 reference seed로 quantile 안정성 확인

> **CDF로 $Y$를 평균하고 reference 입력으로 $X$를 평균하여 population 분위수를 근사**
> 
- Reference 데이터는 training·실제 calibration·최종 평가 데이터와 독립
- Root-finding 중에는 같은 reference 입력을 유지하고 fitted prediction 재사용
- Population 근사에는 conformal 순위 보정 미사용
- 같은 reference 입력을 OLS·RF 비교에 공유 가능
- Root 잔차만 작다고 정확한 population quantile인 것은 아니므로 독립 reference 입력에서 확인
- 시각화용 $21\times21$ grid만으로 population quantile을 정확히 계산했다고 간주하지 않음

True residual $R_0=|\sigma_0(X)\varepsilon|$의 입력 의존성은 $X_1$뿐이므로, error를 CDF로 적분한 뒤 $q_{0,\alpha}$를 별도로 $X_1$에 대한 1차원 수치 적분으로 계산 가능

---

# 6. 핵심 평가지표 — 무엇을 계산하는가

| 구분 | 기본값 | MC와 CP의 전체 차이 | 필수 원인 분해 |
| --- | --- | --- | --- |
| **Coverage** | 구간이 새로운 $Y$를 포함할 확률 | $\operatorname{cov}_{CP,j}-\operatorname{cov}_{MC}$ | Structural·Fitting·Calibration coverage 차이 |
| **구간 중심** | $c_C=(L_C+U_C)/2$ | $c_{CP,j}-c_{MC}$ | Structural·Fitting·Calibration 중심 차이 |
| **구간 반폭** | $w_C=(U_C-L_C)/2$ | $w_{CP,j}-w_{MC}$ | Structural·Fitting·Calibration 반폭 차이 |
| **전체 길이** | $\ell_C=U_C-L_C=2w_C$ | $\ell_{CP,j}-\ell_{MC}$ | 반폭 차이 분해의 2배 |

> 단순히 coverage·중심·폭을 보고 끝내는 것이 아니라 **각 지표의 차이가 어느 단계에서 생기는지 확인하는 것까지 핵심 평가**
> 

**Coverage에서 무엇을 조건부로 고정하는가**

현재 CDF 식의 기본값은 특정 $x$와 이미 구성된 구간을 고정한 coverage

CP에 대해서는 다음과 같이 표시

$$
\operatorname{cov}_{CP,j}^{(b,r)}(x)
=P\!\left\{Y_{new}\in C_{CP,j}^{(b,r)}(x)
\mid X_{new}=x,D_{train}^{(b)},D_{cal}^{(b,r)},\widehat m_j^{(b)}\right\}
$$

RF의 학습 무작위성도 fitted model을 고정하면 함께 고정되며, MC 구간의 coverage는 생성에 사용한 MC 표본도 고정한 값

| 보고값 | 고정 | 평균 대상 |
| --- | --- | --- |
| 고정된 구간의 conditional coverage | $x$, fitted model·calibration | 새로운 $Y\mid X=x$ |
| Calibration 평균 conditional coverage | $x$, fitted model | Calibration 및 새로운 $Y\mid X=x$ |
| Training·calibration 평균 conditional coverage | $x$ | Training·모델 학습 무작위성·calibration 및 새로운 $Y\mid X=x$ |
| Marginal coverage | DGP·실험 설정 | Training·모델 학습 무작위성·calibration 및 새로운 $(X,Y)$ |

따라서 **conditional coverage라는 이름만 쓰지 않고 무엇을 고정·평균했는지 함께 표시**

현재 MC benchmark는 pilot으로 검증한 MC 표본을 평가 중 고정하며, 그 수치 오차는 독립 pilot에서 확인 — 기본 반복 standard error가 MC benchmark 오차까지 포함하는 것은 아님

## 6.1 Coverage 계산 과정

Fitted model·calibration·MC 표본 등 해당 구간을 만드는 정보를 고정

→ 특정 위치 $x=(x_1,x_2)$에서 endpoint $L_C(x),U_C(x)$ 계산

→ true mean과 scale로 표준화

→ true error CDF의 차이 계산

$$
\operatorname{cov}_C(x)
=F_\varepsilon\!\left(\frac{U_C(x)-m_0(x)}{\sigma_0(x)}\right)
-F_\varepsilon\!\left(\frac{L_C(x)-m_0(x)}{\sigma_0(x)}\right)
$$

→ 목표 $1-\alpha$와 비교하여 과소·과대 coverage 확인

→ 같은 위치의 CP와 MC coverage 차이 계산

$$
D_{cov}^{total,MC}(x)=\operatorname{cov}_{CP,j}(x)-\operatorname{cov}_{MC}(x)
$$

> **목표와의 차이**와 **MC와의 차이**는 구분 — 유한 MC에서는 $\operatorname{cov}_{MC}(x)$가 정확히 $1-\alpha$가 아닐 수 있음
> 

현재의 연속 오차분포에서는 CDF로 계산 가능하며, coverage 평가용 $Y$를 추가 반복 생성할 필요 없음

## 6.2 구간 중심·폭 계산 과정

각 구간의 endpoint $L_C(x),U_C(x)$ 사용

→ 중심 $c_C(x)=\{L_C(x)+U_C(x)\}/2$ 계산

→ 반폭 $w_C(x)=\{U_C(x)-L_C(x)\}/2$ 계산

→ 전체 길이 $\ell_C(x)=2w_C(x)$ 계산

→ 각 지표에서 **CP − MC** 차이 계산

$$
D_c^{total,MC}(x)=c_{CP,j}(x)-c_{MC}(x),\qquad
D_w^{total,MC}(x)=w_{CP,j}(x)-w_{MC}(x)
$$

$$
D_\ell^{total,MC}(x)=\ell_{CP,j}(x)-\ell_{MC}(x)=2D_w^{total,MC}(x)
$$

- 중심 차이: 구간이 어느 방향으로 이동하는지
- 반폭·길이 차이: 구간이 얼마나 넓어지거나 좁아지는지
- 반폭과 전체 길이는 같은 폭 정보를 단위만 달리 표현하므로 별개의 독립 지표는 아님
- 평균 중심 차이가 0이어도 위치별 양·음의 차이가 상쇄된 것일 수 있으므로 위치별 차이·반복 변동 함께 확인
- 구간이 짧아도 coverage가 부족하면 더 효율적이라고 판단하지 않음
- Equal-tailed Oracle도 항상 최단 길이 구간인 것은 아님

---

# 7. Coverage 차이의 분해 — 핵심 분석

## 7.1 계산 과정

다섯 구간의 coverage를 **같은 위치·같은 fitted model·같은 calibration 반복**에서 계산

→ Oracle과 true-residual coverage의 차이 계산

→ true-residual과 fitted-population coverage의 차이 계산

→ fitted-population과 finite CP coverage의 차이 계산

→ 세 항에 Oracle–MC coverage 차이를 더하여 전체 CP–MC 차이와 비교

## 7.2 세 항의 정의

| 항 | 정의 | 확인하는 원인 |
| --- | --- | --- |
| **Structural** | $D_{cov}^{struct}(x)=\operatorname{cov}_{res,0}(x)-\operatorname{cov}_{Oracle}(x)$ | True mean을 알아도 하나의 absolute-residual 분위수로 폭을 정하면서 생기는 차이 |
| **Fitting** | $D_{cov}^{fit}(x)=\operatorname{cov}_{pop,j}(x)-\operatorname{cov}_{res,0}(x)$ | True mean 대신 fitted model과 그 residual 분포를 사용하면서 생기는 차이 |
| **Calibration** | $D_{cov}^{cal}(x)=\operatorname{cov}_{CP,j}(x)-\operatorname{cov}_{pop,j}(x)$ | Population 분위수 대신 유한 calibration의 conformal 분위수를 사용하면서 생기는 차이 |

## 7.3 전체 차이와의 관계

$$
\begin{aligned}
D_{cov}^{total,MC}(x)
&=D_{cov}^{struct}(x)+D_{cov}^{fit}(x)+D_{cov}^{cal}(x)\\[4pt]
&\quad+\underbrace{\operatorname{cov}_{Oracle}(x)-\operatorname{cov}_{MC}(x)}_{\text{MC 근사 차이}}
\end{aligned}
$$

- 음수 항: 앞 단계보다 coverage를 낮추는 방향
- 양수 항: 앞 단계보다 coverage를 높이는 방향
- 높아지는 것이 항상 개선은 아니므로 목표 coverage와 함께 해석
- Fitting 항에는 중심 이동뿐 아니라 population residual 분위수 변화도 포함
- Fitting은 유한 training의 추정오차와 model misspecification을 함께 포함하며 두 원인이 이 3단계 분해만으로 따로 분리되는 것은 아님
- Calibration 항에는 유한 표본의 무작위 변동과 conformal 순위 보정의 영향이 함께 포함
- 따라서 fitting을 순수한 표본오차, calibration을 평균 0인 잡음으로 해석하지 않음

> Oracle은 수치 오차를 분리하기 위한 기준점이며 실제 최종 비교는 계속 **MC vs CP**
> 

## 7.4 숫자로 이해하는 예시 — 설명용 가상 값

목표 coverage가 95%인 한 위치에서 다음 값이 나왔다고 가정

| 구간 | Coverage |
| --- | --- |
| Oracle | 0.95 |
| MC | 0.949 |
| True-residual | 0.90 |
| Fitted-population | 0.87 |
| CP | 0.89 |

→ Structural $=-0.05$, fitting $=-0.03$, calibration $=+0.02$, Oracle–MC $=+0.001$

→ 전체 차이 $0.89-0.949=-0.059=-0.05-0.03+0.02+0.001$

**해석:** 이 위치에서는 구조와 fitting이 coverage를 낮추고 calibration 실현은 일부 보상하는 방향

실제 시뮬레이션 결과가 아니며, calibration이 항상 보상한다는 뜻은 아님

---

# 8. 구간 중심·폭 차이의 분해 — 핵심 분석

## 8.1 계산 과정

다섯 구간의 중심·반폭·전체 길이 계산

→ Oracle → true-residual → fitted-population → CP 순서로 각 지표의 차이 계산

→ 각 단계에서 중심이 얼마나 이동하고 폭이 얼마나 달라지는지 확인

→ 같은 단계의 coverage 변화와 연결하여 해석

## 8.2 중심·반폭의 세 항

Error 분위수의 평균과 반간격 정의

$$
a_\alpha=\frac{q_{\alpha/2}(\varepsilon)+q_{1-\alpha/2}(\varepsilon)}2,\qquad
h_\alpha=\frac{q_{1-\alpha/2}(\varepsilon)-q_{\alpha/2}(\varepsilon)}2
$$

Oracle의 중심은 $m_0(x)+\sigma_0(x)a_\alpha$, 반폭은 $\sigma_0(x)h_\alpha$

| 분해 항 | 중심 차이 | 반폭 차이 |
| --- | --- | --- |
| **Structural** | $D_c^{struct}(x)=-\sigma_0(x)a_\alpha$ | $D_w^{struct}(x)=q_{0,\alpha}-\sigma_0(x)h_\alpha$ |
| **Fitting** | $D_c^{fit}(x)=\widehat m_j(x)-m_0(x)$ | $D_w^{fit}(x)=q_{pop,j,\alpha}-q_{0,\alpha}$ |
| **Calibration** | $D_c^{cal}(x)=0$ | $D_w^{cal}(x)=\widehat q_{j,\alpha}-q_{pop,j,\alpha}$ |

→ 중심 차이는 중심 항끼리, 반폭 차이는 반폭 항끼리 합산

$$
D_c^{total,MC}
=D_c^{struct}+D_c^{fit}+D_c^{cal}+(c_{Oracle}-c_{MC})
$$

$$
D_w^{total,MC}
=D_w^{struct}+D_w^{fit}+D_w^{cal}+(w_{Oracle}-w_{MC})
$$

- 대칭 오차에서는 $a_\alpha=0$이므로 structural 중심 차이는 0
- 비대칭 오차에서는 true mean과 Oracle 중심이 다를 수 있으므로 structural 중심 차이도 존재 가능
- Calibration은 같은 fitted model의 폭만 바꾸므로 중심 차이는 0
- CP–MC의 중심·폭 차이가 각각 어느 단계에서 발생하는지 coverage 차이와 연결하여 해석

## 8.3 전체 길이의 세 항

| 항 | 정의 |
| --- | --- |
| **Structural** | $D_\ell^{struct}(x)=\ell_{res,0}(x)-\ell_{Oracle}(x)$ |
| **Fitting** | $D_\ell^{fit}(x)=\ell_{pop,j}(x)-\ell_{res,0}(x)$ |
| **Calibration** | $D_\ell^{cal}(x)=\ell_{CP,j}(x)-\ell_{pop,j}(x)$ |

$$
\begin{aligned}
D_\ell^{total,MC}(x)
&=D_\ell^{struct}(x)+D_\ell^{fit}(x)+D_\ell^{cal}(x)\\[4pt]
&\quad+\underbrace{\ell_{Oracle}(x)-\ell_{MC}(x)}_{\text{MC 근사 차이}}
\end{aligned}
$$

앞에서 정의한 $h_\alpha$를 사용하면

| 항 | 구체적인 길이 차이 |
| --- | --- |
| Structural | $2\{q_{0,\alpha}-\sigma_0(x)h_\alpha\}$ |
| Fitting | $2(q_{pop,j,\alpha}-q_{0,\alpha})$ |
| Calibration | $2(\widehat q_{j,\alpha}-q_{pop,j,\alpha})$ |

현재는 **전체 길이**의 차이이므로 half-width 차이에 비해 계수 2가 붙는 점에 주의

한 fitted model·calibration 아래 fitting·calibration 길이 차이는 $x$에 무관하지만, 해당 단계의 coverage 차이는 $x$에 따라 달라질 수 있음

## 8.4 분해의 공통 해석 규칙

- 각 지표 모두 **부호 있는 차이**의 정확한 telescoping 분해
- 세 항은 서로 상쇄 가능하며 독립적인 인과적 기여율이 아님
- 각 항의 절댓값 합은 일반적으로 전체 차이의 절댓값과 다름
- 평균은 선형이므로 같은 가중치로 각 항을 평균해도 signed 분해식 유지
- 각 항의 분산 합이 전체 분산은 아님 — 항 사이 공분산 존재 가능
- Endpoint·길이 차이를 coverage 차이로 선형 변환하지 않음 — 각 구간의 CDF coverage를 직접 계산
- Population 분위수의 수치 근사오차는 별도로 검증하여 fitting·calibration 결과를 과도하게 해석하지 않음

---

# 9. 세 평가 관점에서 무엇을 보고하는가

## 9.1 한눈에 정리

| 관점 | 고정 | 평균·변화 | 핵심 보고값 |
| --- | --- | --- | --- |
| **Marginal** | DGP·실험 설정 | 새로운 $X$, training·calibration | 평균 coverage·중심·반폭·길이 및 각 지표의 평균 분해 항 |
| **Fixed-$x$** | Fitted model·$x_0$ | Calibration 반복 | Coverage·중심·폭·분해 항의 평균과 변동 |
| **Fixed-calibration** | Fitted model·calibration | 평가 위치 $x$ | 위치별 coverage·중심·폭·분해 항 |

각 관점에서 기본값과 CP–MC 전체 차이, 세 분해 항, MC 근사 차이를 함께 보고

## 9.2 Marginal 계산 과정

한 training·calibration 반복 고정

→ 독립 평가 입력 $X^{eval}_1,\ldots,X^{eval}_{N_{eval}}$ 생성

→ 각 입력에서 다섯 구간의 coverage·중심·폭과 모든 분해 항 계산

→ 입력에 대해 평균

→ $b=1,\ldots,B$의 training·calibration 반복까지 평균

예를 들어 한 반복의 coverage 평균은

$$
v_C^{(b)}=E_X[\operatorname{cov}_C^{(b)}(X)]
=\frac14\int_{-1}^{1}\int_{-1}^{1}\operatorname{cov}_C^{(b)}(x_1,x_2)\,dx_2\,dx_1
$$

실제로는 독립 평가 입력의 평균으로 근사하고, 최종 marginal coverage는 $B^{-1}\sum_b\widehat v_C^{(b)}$로 추정

평균 중심·반폭·길이와 각 분해 항도 같은 평가 입력·같은 가중치로 평균

- 같은 $b$ 안에서는 평가 입력을 방법 간 공유
- 평가 입력은 population reference 입력과 독립
- 서로 다른 $b$의 평가 입력은 독립 생성
- $21\times21$ grid의 단순 평균을 정확한 marginal 적분으로 간주하지 않음
- 각 반복 요약값의 standard deviation을 $\sqrt B$로 나눠 Monte Carlo standard error 계산
- 방법 간 차이도 각 반복에서 먼저 구한 뒤 그 차이의 standard error 계산

### Marginal coverage 분해에서 특히 중요한 성질

현재 연속 DGP의 **정확한 population 분위수**를 사용하면

$$
E_X[\operatorname{cov}_{Oracle}(X)]
=E_X[\operatorname{cov}_{res,0}(X)]
=E_X[\operatorname{cov}_{pop,j}(X)\mid\widehat m_j]
=1-\alpha
$$

따라서 fitted model이 고정되어 있어도

$$
E_X[D_{cov}^{struct}(X)]=0,\qquad
E_X[D_{cov}^{fit}(X)\mid\widehat m_j]=0
$$

> **Marginal coverage의 structural·fitting 차이가 0이어도 위치별 문제가 없다는 뜻은 아님 — 과소·과대 coverage가 평균에서 상쇄 가능**
> 

그래서 위치별 coverage 분해를 반드시 함께 분석

이 0이라는 성질은 **coverage**에 대한 것이며 marginal 길이의 structural·fitting 차이가 0이라는 뜻은 아님

Finite calibration은 고정된 실현마다 평균 coverage가 달라질 수 있으며, calibration까지 평균한 CP coverage는 현재 조건에서 $k_\alpha/(m+1)$

Population·평가 입력의 유한 근사에서는 위 이론적 0에 작은 수치 차이가 생길 수 있음

## 9.3 Fixed-$x$ 계산 과정

각 DGP에서 사전 지정한 $b=1,\ldots,10$ 중 하나의 fitted model 고정

→ 위치 $x_0=(x_{01},x_{02})$ 고정

→ calibration 1000개 생성 후 CP 분위수·구간 계산

→ 다섯 구간의 coverage·중심·폭·각 분해 항 계산

→ 기본 $r=1$을 포함하여 calibration 총 $R_{cal}=500$회 평가

→ 각 값의 평균·표준편차·반복 분위수 요약

대표 위치는 각 좌표 $\{-0.9,-0.5,0,0.5,0.9\}$의 25개 조합을 초기 설정으로 사용

- 이 반복에서 structural·fitting 항은 고정
- Coverage·반폭·길이의 calibration 항과 CP–MC 전체 차이는 calibration에 따라 변동 가능
- 같은 fitted model·평가 위치·MC benchmark를 고정하면 CP 중심과 CP–MC 중심 차이는 calibration 반복에 대해 불변이며, 중심의 calibration 항은 항상 0
- MC 표본도 고정하여 MC 변동과 calibration 변동을 혼합하지 않음
- 위 10개 training ID마다 conditional 결과를 별도로 보고
- Training·calibration 평균의 fixed-$x$ 결과는 기본 $B=200$개 반복의 같은 위치에서 계산한 값을 평균하여 별도 보고
- 한 training에 calibration이 하나뿐이어도 $B$개의 독립 반복 평균으로 training·calibration에 대한 기대값을 추정 가능하나, 한 모델의 calibration 변동을 따로 평가하려면 위 500회 반복 필요
- 200회 평균과 각 모델의 500회 평균·변동은 서로 다른 조건부 대상을 평가하므로 하나의 숫자로 혼합하지 않음
- 동일 fitted model의 calibration 500회를 독립 training 500회로 간주하지 않음

## 9.4 Fixed-calibration 계산 과정

한 fitted model과 calibration set 고정

→ 관심 있는 각 위치 $x$에서 다섯 구간의 coverage·중심·폭 계산

→ 각 위치의 CP–MC 차이와 모든 분해 항 계산

→ 위치별 coverage 변화와 중심 이동·폭 변화를 연결하여 확인

→ 다른 training·calibration 반복에서도 같은 위치에서 평가

개별 반복 결과와 반복 평균 결과를 구분하고, 평균에서 상쇄되는 변동을 확인하기 위해 위치별 반복 표준편차 또는 분위수 함께 보고

---

# 10. Heatmap과 추가 진단 — 지표 계산 이후

## 10.1 Grid와 heatmap

각 축 $-1,-0.9,\ldots,0.9,1$로 $21\times21=441$개 평가점 구성

→ 먼저 §6–9의 수치 결과를 표 형태로 저장

→ 가로축 $x_1$, 세로축 $x_2$에 계산 결과를 배열

→ Coverage·중심·폭 및 분해 항의 heatmap 작성

| 그림 | 표시하는 값 |
| --- | --- |
| 기본 coverage | 각 구간의 coverage 또는 목표와의 차이 |
| MC–CP 비교 | CP − MC의 coverage·중심·폭 차이 |
| 원인 분해 | Structural·Fitting·Calibration 및 MC 근사 차이 |

비교하는 패널에는 동일 color scale 사용, signed 차이는 0 중심 색상 사용

대표 반복과 반복 평균 그림을 구분하며, 반복 분위수는 평균 신뢰구간이나 동시 신뢰대가 아님

대표 조건에서 $41\times41$로 세분화하여 중요한 패턴을 놓치지 않는지 확인하는 후속 점검 가능

## 10.2 필요할 때 추가하는 진단

- 추가적인 endpoint 차이
- Length ratio
- 평균 undercoverage 미달량·미달 영역 비율
- 정규화 endpoint discrepancy

위 값들은 원인 설명을 보완하는 선택적 지표이며, **coverage·중심·폭과 각 지표의 필수 분해를 대체하지 않음**

---

# 11. 실제 실행 순서

구간 생성 함수 구현·검증 → 구간 관련 pilot과 결과 확인 → 지표·분해·반복 분석 구현 → 평가 표본 수 검증 → 본 실행 순서

## Step 1. 사전 설정

2차원 DGP·RF 세부 설정·seed 확정

→ 세 관점의 평균 대상을 확인하고 fixed-$x$ training ID $b=1,\ldots,10$ 적용

→ 구간 생성 함수 구현·검증 후 $M_{MC}$·$M_{pop}$ 및 구간 관련 허용오차 pilot

→ $N_{eval}$은 marginal 평가 구현 후 별도로 pilot하고 본 실행 전에 확정

## Step 2. 한 반복의 다섯 구간 구성

Training 1000개 생성 → OLS·RF 학습

→ 독립 calibration 1000개 생성 → CP 분위수 계산

→ fitted-population 분위수 계산

→ 미리 계산한 Oracle·MC·true-residual과 함께 다섯 구간 구성

## Step 3. 핵심 지표와 분해

같은 위치·평가 입력에서 다섯 구간의 coverage·중심·폭 계산

→ CP − MC 전체 차이 계산

→ 각 지표의 structural·fitting·calibration 항 계산

→ MC 근사 차이 계산

→ 각 분해 항의 합이 전체 signed 차이와 일치하는지 확인

## Step 4. Marginal·fixed-calibration 반복 요약

지표·분해·반복·저장 구현 및 작은 실행 검증 → 독립 평가 입력 수 $N_{eval}$의 안정성 확인

→ Step 2–3을 $B=200$회 반복

→ Marginal 기본값·차이·분해 항의 평균과 standard error 보고

→ Fixed-calibration 위치별 기본값·차이·분해 항 저장

→ 개별 반복과 반복 평균·변동 구분

## Step 5. Fixed-$x$ 반복

사전 지정한 10개 training ID에서 각각 fitted model·평가점 고정

→ 각 fitted model에서 기본 calibration을 포함한 총 500회 평가

→ Coverage·중심·폭·분해 항의 평균과 변동 보고

## Step 6. 결과 표현과 추가 분석

핵심 평가표 작성 → 이후 heatmap 작성

→ 필요 시 추가 endpoint 진단

→ 시간이 허용되면 sample-size sensitivity 수행

> **Step 3의 오차 분해는 필수이며 뒤로 미루는 선택적 분석이 아님**
> 

---

# 12. Sample-size sensitivity와 보류 항목

| 항목 | 설정 | 우선순위 |
| --- | --- | --- |
| Training-size | $n\in\{250,500,1000,2000\}$, CP 비교 시 $m=1000$ | 선택적 보조 |
| Calibration-size | $m\in\{250,500,1000,2000,4000\}$, $n=1000$ | 선택적 보조 |
| 차원 확장 | $d=1,5,10$ 비교 | 보류 |
| 추가 방법 | CQR·normalized CP·Jackknife+ | 제외 |
| 전체 factorial | 모든 $n\times m\times d$ 조합 | 제외 |

대표 조건은 linear–homo–Gaussian, nonlinear–homo–Gaussian, linear–hetero–Gaussian, nonlinear–hetero–LogNormal

$n$ 증가에 따른 fitting 개선과 $m$ 증가에 따른 calibration 안정화는 실험으로 확인할 경향이며, 모든 반복에서 단조 개선된다는 보장은 없음

---

# 13. 구현 확인과 해석 주의점

| 점검 | 확인 내용 |
| --- | --- |
| Oracle coverage | 모든 평가 위치에서 목표 coverage |
| MC 정확도 | Oracle 대비 endpoint·coverage 오차가 정한 허용범위 이내 |
| CP 순위 | $m=1000$에서 901·951·991번째 residual |
| Population 정확도 | 독립 reference와 표본 확대에서 분위수 안정성 |
| 대칭·등분산 | $C_{Oracle}=C_{res,0}$ |
| CP 반폭·길이 | 한 반복의 모든 위치에서 일정 |
| Calibration center 차이 | 0 |
| Fitting half-width | 대칭 unimodal 오차에서 수치 오차 범위 내 비음수 |
| 분해 등식 | Coverage·중심·반폭·길이 각각에서 세 항 + MC 항 = CP − MC |
| Marginal structural·fitting coverage | 정확한 population 기준에서 0, 근사 오차 고려 |
| Marginal coverage | 반복 standard error를 고려해 이론적 coverage와 비교 |

Gaussian·Student-$t$의 fitting half-width 부호는 같은 길이의 구간 중 대칭분포의 중심 구간이 가장 큰 확률질량을 갖는 성질에서 도출

따라서 fixed model에서 fitted absolute residual CDF가 true residual CDF보다 클 수 없고 $q_{pop,j,\alpha}\ge q_{0,\alpha}$ — LogNormal에는 이 부호 조건 적용 불가

Telescoping 등식만으로 population quantile이나 DGP 구현의 정확성을 검증할 수 없으므로 위 점검들을 별도 수행

**해석의 범위**

- Marginal coverage가 맞아도 모든 위치의 conditional coverage가 맞는 것은 아님
- Grid는 유한한 평가점이며 grid 사이 전체 영역의 coverage를 보장하지 않음
- Grid 최솟값은 전체 연속 입력 공간의 최악 coverage와 다를 수 있음
- 평균 heatmap은 특정 calibration set의 성능을 보장하지 않음
- 모델·calibration 반복의 변동과 MC·population·평가 입력의 수치 근사오차를 구분
- 현재 2차원 결과를 고차원 일반 성능으로 확대 해석하지 않음

---

# 14. 최종 산출물과 근거

| 순서 | 필수 산출물 | 목적 |
| --- | --- | --- |
| 1 | 다섯 구간의 coverage·구간 중심·반폭·전체 길이 | 각 단계의 기본 성능 확인 |
| 2 | CP − MC coverage·중심·폭 차이 | 실제 비교 대상의 차이 확인 |
| 3 | 각 지표의 structural·fitting·calibration 항 + MC 항 | 차이가 발생한 단계와 방향 설명 |
| 4 | Marginal·fixed-$x$·fixed-calibration별 요약 | 평균·calibration 변동·위치별 문제 구분 |
| 5 | 위 수치 결과의 표·heatmap | 계산 결과 표현 |

**근거**

- 현재 대화에서 확정한 방향: 두 설명변수, coverage 우선, coverage·중심·폭의 원인 분해 필수, heatmap은 후속 표현
- [1] Angelopoulos & Bates, [A Gentle Introduction to Conformal Prediction and Distribution-Free Uncertainty Quantification](https://arxiv.org/abs/2107.07511), §1 및 Appendix D — Split CP 보정 분위수와 marginal coverage 보장
- 2차원 mean·grid·대표 평가점은 본 문서의 설계안
- CDF coverage·population 계산식·mean 분산·signed decomposition은 명시한 DGP에서 직접 도출

**남은 실행 설정:** RF 세부 하이퍼파라미터·구현 버전, reference·평가 표본 수, 수치 허용오차

실제 코드를 실행한 결과가 아니므로 실행시간·수치 정밀도·coverage 결과는 아직 확인되지 않은 상태