# 시뮬레이션 설계 - 동일한 fitted model의 MC vs Split CP

> **핵심 질문**
> 
> 
> 동일한 fitted model을 사용할 때, 추정한 정규 오차분포로 구성한 MC 예측구간과 별도 calibration residual로 구성한 CP 예측구간은 실제 새로운 반응값을 얼마나 포함하는가
> 
> **Random Forest가 true 함수를 잘 근사하는지 확인 → 잔차분산 추정 → 같은 fitted model의 MC·CP 구간 생성 → coverage·길이 비교**
> 
- 연구 목적 : true 함수를 충분히 잘 근사하는 fitted model을 확보, **그 모델 하나를 고정**한 상태에서 MC · CP 예측구간의 coverage와 길이를 비교
    - “모델에 따른 차이”는 비교 대상 X, MC와 CP가 정확히 같은 $\hat f$, 같은 fitting
    - error를 공유하게 만들어서 **구간 구성 방식(정규 가정 vs empirical residual)만의 차이**를 보는 것이 설계 의도

---

# 0. 이 문서에서 가장 헷갈리기 쉬운 지점 - 먼저 정리

시뮬레이션을 짜기 전에 아래 세 가지를 확실히 구분해야 함(구분이 안 되면 “모델을 1000번 학습해야 하나?” 같은 혼동)

| 숫자 | 정체 | 몇 번 등장하는가 |
| --- | --- | --- |
| $n_{train}=1000$ | training **행(observation) 개수** | 데이터 크기. 학습 횟수가 아님 |
| 모델 학습 | Random Forest를 데이터에 맞추는 행위 | **DGP 하나당 정확히 1번**. 재학습 없음 |
| $n_{cal}=1000$ | calibration **행 개수** | 데이터 크기. CP 분위수 계산도 DGP당 1번 |
| $M_{MC}=1000$ | 구간 **하나**를 만들 때 뽑는 가상 $Y^*$ 개수 | 재학습과 무관. 한 반복 $r$ 안에서 쓰는 표본 수 |
| $R_{MC}=200$ | “가상 $Y^*$ 1000개 뽑기 → 구간 하나 → 새 true $Y$ 하나 → 포함여부 체크”를 **반복하는 횟수** | coverage를 비율로 추정하기 위한 반복. 학습과 무관 |

<aside>
⚠️

**결론 : 모델 학습은 DGP당 1번뿐**

“동일한 fitted model을 사용할 때”라는 전제는, MC와 CP가 서로 다른 모델에서 나온 결과가 아니라 **완전히 같은 $\hat f$, 같은 $\hat\sigma^2$**을 공유해야 함

모델을 여러 번 재학습하면 그때마다 $\hat f$가 달라지고, 그 fitting variation이 MC·CP coverage 둘 다에 섞여 들어가 버려서 “구간 구성 방식만의 차이”를 보려는 목적과 어긋남

(모델 재학습 자체를 반복 요인으로 넣는 설계는 완전히 다른 연구 질문이며, 11절 “전체 실험 반복 $B$”에서 별도로 다룸 → 현재는 X)

</aside>

## 0.1 MC와 CP는 “구간을 언제 다시 구하는가”가 다르다

- **CP 구간** : calibration set(1000개)에서 잔차 분위수 $\hat q_\alpha$를 **딱 1번** 계산하면, 이후 $C_{CP}(X_i) = \hat f_j(X_i) \pm \hat q_\alpha$는 **200번 반복 $r$ 동안 전혀 안 바뀌는 상수**(calibration을 “전체 세트 고정”으로 쓸 경우. 6.4 참고)
- **MC 구간** : 매 반복 $r$마다 $Y^*$ 1000개를 **새로** 뽑고, 그 표본의 empirical quantile로 구간을 **매번 새로 계산**
    - $\hat f_j(X_i)$와 $\hat\sigma_j^2$는 안 바뀌지만, 유한 표본(1000개)에서 뽑은 quantile이라 표본추출 변동 때문에 $r$마다 구간이 미세하게 흔들림
    

그래서 200번 반복 동안:

- CP는 “새 true $Y^{test}$ 변동”만 평균내는 것
- MC는 “MC 표본추출 변동 + 새 true $Y^{test}$ 변동” 둘 다를 평균내는 것

이 비대칭이 존재한다는 것을 알고 있어야, 왜 MC 파트가 계산량도 크고 반복이 꼭 필요한지 이해가 된다 (CP는 사실 구간이 고정이라 반복해도 새 $Y^{test}$만 바뀌는 것).

---

# 1. 확정 사항과 미정 사항

| 항목 | 설정 | 상태 |
| --- | --- | --- |
| 설명변수 | 두 변수 $X_1,X_2$ | 확정 |
| Training 크기 | $n_{train}=1000$ | 확정 |
| Calibration 크기 | $n_{cal}=1000$, training과 1:1 | 확정 |
| Training 입력 | 미리 정한 입력들을 사용하며 MC·평가에도 같은 위치 사용 | 확정 |
| True model | $Y=f(X)+\sigma_0(X)\varepsilon$ | 12개 DGP 조합 |
| Fitted model | **DGP당 한 번** 학습한 동일한 모델을 MC와 CP에서 공유, 재학습 없음 | 확정 |
| MC 오차 | Training residual $r_i$로 추정한 $N(0,\widehat\sigma^2)$ | 확정 |
| MC 생성 수 | 각 입력에서 $M_{MC}=1000$ | 현재 지정값, 충분성 검증 전 |
| Calibration 생성 | Training 입력 재사용 없이 별도 입력·true model로 생성 | 확정 |
| Calibration 반복 정책 | **전체 세트 고정** (200회 동안 재생성 안 함) | 잠정 확정 (0.1절 근거) |
| Coverage | True model의 새로운 반응값에 대한 실제 포함 비율 | 확정 |
| 모델 정확도 | 구간 비교에 앞서 $\widehat f$와 true $f$의 근사 정확도 확인 | 첫 단계 |
| 모델 | Random Forest | 우선 사용, 하이퍼파라미터 미정 |
| 모델 확장 | Deep learning 등 | 후속 추가 가능 |
| 근사 정확도 기준 | 충분히 잘 맞춘다고 판단할 지표·허용 수준 | 미정 |
| 목표 coverage | 90%, 95%, 99% | 비교 수준 |
| DGP 조건 | 2 mean × 2 scale × 3 error | 확정 |
| 입력 생성·배치 | 두 좌표를 독립 $U(-1,1)$에서 생성, training 입력은 생성 후 고정 | 구체적 seed 미정 |
| 분산 추정 세부 규칙 | 모델별 분모·보정 (우선 $n^{-1}$ 단순 평균으로 시작) | 미정 |
| MC 구간 반복 수 | 입력별 $R_{MC}=200$ | 임시 설정 |
| True 평가값 | 각 입력·반복에서 새 반응값 하나, 총 200개 | 구간과 1:1 대응 |
| 전체 실험 반복 | 반복 수 $B$와 고정·재생성 정책 | 미정, 우선 $B=1$ |
| 모델 학습 횟수 | **DGP당 1회** - 200회 MC 반복이나 calibration 계산과 무관하게 재학습 없음 | 확정 (0절 근거) |
| 계산량 우려 | MC 파트가 CP보다 계산량이 훨씬 크므로 시간이 오래 걸릴 것으로 예상됨
(교수님 언급) → chunk 단위 처리 필요 | 확정, 11절 참고 |

# 2. DGP — 12개 조건

True model은 $Y=f(X)+\sigma_0(X)\varepsilon$이며, $E[\varepsilon]=0$, $\operatorname{Var}(\varepsilon)=1$

## 2.1 입력분포

$X_1,X_2\overset{iid}{\sim}U(-1,1)$이며 입력과 오차는 독립

Training 입력 1,000행을 한 번 생성하여 고정하고, MC와 평가에서도 같은 입력 사용

Calibration 입력은 같은 입력분포에서 별도로 1,000행 생성

DGP·모델 간 비교에는 동일한 training 입력 설계 사용 가능하며, 실제 공유 범위는 설정에 기록

## 2.2 True mean

| Mean | $f(x)$ |
| --- | --- |
| Linear | $\sqrt2(x_1+x_2)$ |
| Nonlinear | $\sqrt{6/11}\{2\sin(\pi x_1)+x_2+x_1x_2\}$ |

지정한 입력분포에서 두 함수 모두 평균 0, 분산 $4/3$

Random Forest로 linear·nonlinear mean을 각각 학습하고, 조건별 함수 근사 정확도를 먼저 확인

## 2.3 True scale

| Scale | $\sigma_0(x)$ |
| --- | --- |
| Homo | $1$ |
| Hetero | $\{0.4+1.2\sin^2(\pi x_1)\}/\sqrt{1.18}$ |

두 조건 모두 입력분포에 대한 평균 noise variance $E_X[\sigma_0^2(X)]=1$

이는 모집단 입력분포 기준이며, 고정된 유한 입력의 평균 $n^{-1}\sum_i\sigma_0^2(X_i)$가 정확히 1일 필요는 없음

## 2.4 표준화 오차분포

| Error | 생성식 | 특징 |
| --- | --- | --- |
| Gaussian | $\varepsilon\sim N(0,1)$ | 대칭 정규 |
| Student-t | $\varepsilon=T/\sqrt3$, $T\sim t_3$ | 대칭 비정규·heavy tail |
| LogNormal | $\varepsilon=(e^Z-e^{1/2})/\sqrt{e(e-1)}$, $Z\sim N(0,1)$ | 비대칭 비정규 |

모든 error의 평균 0, 분산 1이며 training·calibration·평가에 동일한 scenario의 분포 사용

**2 mean × 2 scale × 3 error = 12개 DGP**

Random Forest 기준 12개 model–DGP 조합이며, 3개 목표 coverage별로 36개 결과 조건

추후 모델이 추가되어도 DGP·평가 입력·평가지표는 공통 절차 사용

## 2.5 True DGP와 MC 생성분포의 구분 - 이 연구가 보려는 핵심 대비

| 대상 | 사용 분포 |
| --- | --- |
| Training·calibration·true 평가 반응값 | 선택한 $f$, $\sigma_0$, $F_\varepsilon$ |
| MC 가상 반응값 | $N(\widehat f_j(x),\widehat\sigma_j^2)$ |

MC는 training residual에서 추정한 **하나의 상수 분산**으로 Gaussian 가상 오차를 생성하는 방식

이분산($\sigma_0(x)$가 $x$에 따라 변함)·비정규($t_3$, $LogNormal$) DGP에서도 MC는 이 사실을 전혀 반영 X → MC는 “오차가 등분산이고 정규분포를 따른다”는 가정을 무조건 사용

but, CP는 calibration residual의 **empirical 분포**를 그대로 사용 → 실제 오차가 이분산이든 비정규든 그 분포 정보가 (marginal하게는) 잔차 분위수에 반영

⇒ 조건별 비교에는 **MC의 등분산·정규 가정이 실제 DGP와 맞는지에 따른 영향**이 포함(구체적으로 예상되는 패턴)

- Homo + Gaussian : MC 가정이 DGP와 정확히 일치 → MC coverage가 목표치에 가장 가까울 것으로 예상
- Hetero 조건 : MC가 위치별 분산 차이를 못 잡음 → 분산이 큰 위치에서는 MC 구간이 너무 좁아 coverage 부족, 작은 위치에서는 너무 넓어 coverage 과다 예상
- 비정규(t3, LogNormal) 조건 : MC의 정규가정이 tail이나 비대칭성을 못 잡음 → 특히 99% 같은 극단 분위수에서 괴리가 커질 것으로 예상

(위 예상은 시뮬레이션 결과로 확인해야 할 가설이며, 미리 결론으로 단정하지 않음)

MC의 오차분포나 분산함수까지 DGP에 맞춰 추정하는 방법은 별도의 방법 정의가 필요하며 현재 절차에 포함하지 않음

# 3. 입력·데이터·기호

## 3.1 입력 고정의 의미

Training 입력행렬을 $X^{train}=(X_1^T,\ldots,X_n^T)^T$로 표기하고, 각 행은 $X_i=(x_{i1},x_{i2})$

- $X^{train}$의 크기는 $1000\times2$
- 각 행에 두 설명변수의 값을 배치하여 고정된 1,000행의 입력 설계 구성
- 두 열은 독립 균등분포에서 생성하고 입력값과 seed 저장
- 주 평가 위치는 $x_i=X_i^{train}$이며, MC 생성과 새 반응값 평가 모두 같은 위치에서 수행
- 입력을 재사용해도 training의 관측 반응값 $Y_i$를 평가에 재사용하지 않음
- 여러 실험 반복 사이에서도 같은 입력행렬을 유지하는 범위는 실행 설정에 명시

## 3.2 서로 다른 세 종류의 반응값

| 구분 | 생성식 | 목적 |
| --- | --- | --- |
| Training 반응값 | $Y_i^{train}=f(X_i)+\sigma_0(X_i)\varepsilon_i^{train}$ | 모델 학습과 잔차분산 추정 |
| MC 가상 반응값 | $Y_{i,r,s}^*=\widehat f(X_i)+\epsilon_{i,r,s}^*$ | MC 구간 구성 |
| True 평가 반응값 | $Y_{i,r}^{test}=f(X_i)+\sigma_0(X_i)\varepsilon_{i,r}^{test}$ | MC·CP coverage 평가 |

Training·평가의 $\varepsilon$은 해당 DGP의 표준화 오차분포, MC 오차는 추정한 $N(0,\widehat\sigma^2)$ 사용

Calibration 반응값은 별도 입력 $X_a^{cal}$에서 $Y_a^{cal}=f(X_a^{cal})+\sigma_0(X_a^{cal})\varepsilon_a^{cal}$로 생성

Training·calibration·평가의 true 오차와 MC 난수는 독립적으로 생성

## 3.3 잔차 표기 - $r_i$ vs $R_a$ (혼동 방지)

| 기호 | 정체 | 계산식 | 쓰이는 곳 |
| --- | --- | --- | --- |
| $r_i$ (= $e_{i,j}$) | **Training residual** (부호 있음) | $r_i = Y_i^{train} - \widehat f_j(X_i)$ | $\widehat\sigma_j^2$ 추정 (MC용) |
| $R_a$ | **Calibration absolute residual** | $R_{a,j} = \lvert Y_a^{cal}-\widehat f_j(X_a^{cal})\rvert$ | $\widehat q_{j,\alpha}$ 계산 (CP용) |

- $r_i$는 training set(1000개)에서, $R_a$는 calibration set(1000개)에서 계산되며 서로 다른 데이터를 사용
- $\widehat\sigma^2$를 구할 때 쓰는 잔차는 **training residual $r_i$**이며, calibration residual $R_a$가 아님

## 3.4 인덱스 기호 정리

| 기호 | 의미 |
| --- | --- |
| $j$ | 모델 종류, 현재 Random Forest |
| $i=1,\ldots,1000$ | Training 입력 및 주 평가 위치 |
| $a=1,\ldots,1000$ | 별도 calibration 관측치 |
| $s=1,\ldots,M_{MC}$ | 한 입력에서 MC 가상 반응값 생성 ($M_{MC}=1000$) |
| $r=1,\ldots,R_{MC}$ | MC 구간 생성·포함 여부 평가 반복 ($R_{MC}=200$, 임시) |
| $b$ | 전체 실험 반복을 도입할 경우의 반복 ID (현재 미도입) |
| $c$ | Calibration 세트 ID, 고정이면 동일 ID 유지 (현재 정책: 고정) |

# 4. 한 실험(한 DGP)의 전체 과정 — 실행 순서 명세

아래는 DGP 하나에 대해 실제로 코드를 짤 때 그대로 따라야 할 순서다. 각 단계가 “몇 번 실행되는지”를 명시했다.

```
[DGP당 1회 실행]
1.  Training 입력 X^train 사용 (1단계에서 전체 실험 공통으로 이미 고정됨)
2.  true model로 training 반응값 Y^train 1,000개 생성
        Y_i^train = f(X_i) + sigma0(X_i) * eps_i^train
3.  Random Forest 학습 → f_hat_j 확보                       ★모델 학습, DGP당 딱 1번★
4.  true f와 비교하여 함수 근사 정확도 확인
        delta_i = f_hat_j(X_i) - f(X_i)
        함수 RMSE, MAE, 오차분위수 계산·기록
5.  정확도가 부족한 조건이면 적합 상태·모델 설정 점검, 기준 미정이므로 일단 기록만
6.  Training residual r_i = Y_i^train - f_hat_j(X_i) 계산
7.  r_i로 sigma_hat_j^2 추정 (n^-1 sum r_i^2, 규칙 잠정)      ★분산 추정, DGP당 딱 1번★
8.  Calibration 입력 X^cal 사용 (1단계에서 이미 고정됨, training과 별도)
9.  true model로 calibration 반응값 Y^cal 1,000개 생성
10. 같은 f_hat_j로 calibration absolute residual R_a 계산
        R_a = |Y_a^cal - f_hat_j(X_a^cal)|
11. R_a 정렬 후 각 alpha(0.10, 0.05, 0.01)별 k_alpha번째 값 = q_hat_alpha  ★CP 분위수, DGP당 딱 1번★
12. CP 구간이 이 시점에 모든 X_i에 대해 이미 확정됨
        C_CP(X_i) = [f_hat_j(X_i) - q_hat_alpha, f_hat_j(X_i) + q_hat_alpha]
        (200번 반복 동안 이 값은 상수로 재사용, 0.1절 참고)

[여기서부터 r = 1, ..., 200 반복]
13. 각 입력 X_i에서 MC 가상 반응값 M_MC=1000개 새로 생성
        eps*_{i,r,s} ~ N(0, sigma_hat_j^2),  s = 1,...,1000
        Y*_{i,r,s} = f_hat_j(X_i) + eps*_{i,r,s}          ★모델 재학습 없음, f_hat_j 재사용★
14. 이 1,000개의 양쪽 empirical quantile로 MC 구간 구성 (alpha별로 동시 계산)
        C_MC^(r)(X_i) = [Q_hat_{alpha/2}, Q_hat_{1-alpha/2}]
15. True 평가 반응값 하나를 MC 표본과 독립적으로 생성
        Y_{i,r}^test = f(X_i) + sigma0(X_i) * eps_{i,r}^test
16. 포함 여부 지시함수 기록
        I_MC[i,r,alpha] = 1{ Y_{i,r}^test in C_MC^(r)(X_i) }
        I_CP[i,r,alpha] = 1{ Y_{i,r}^test in C_CP(X_i) }        (12에서 이미 계산된 상수 구간 사용)
[반복 종료, r=200까지]

17. 입력별 coverage 계산
        cov_MC(X_i) = (1/200) sum_r I_MC[i,r,alpha]
        cov_CP(X_i) = (1/200) sum_r I_CP[i,r,alpha]
18. 구간 길이, fitted model 정확도, 분산 추정값 저장
```

**요약**: 3, 7, 11번(학습·분산추정·CP분위수)은 DGP당 **딱 1번**. 13~16번(MC 표본생성·구간구성·평가)만 **200번** 반복된다. 이 비대칭이 계산량 대부분을 MC 파트에 몰아넣는 이유다 (11절 참고).

# 5. 첫 단계 - Random Forest의 함수 근사 정확도

## 5.1 무엇을 먼저 확인하는가

각 DGP에서 training data로 Random Forest를 학습한 뒤, $\widehat f_j(X_i)$와 true $f(X_i)$를 직접 비교

**관측된 $Y_i$를 잘 맞추는지보다 true 함수 $f$를 잘 근사하는지 먼저 확인**

| 확인 항목 | 계산·표현 | 의미 |
| --- | --- | --- |
| 위치별 오차 | $\delta_j(X_i)=\widehat f_j(X_i)-f(X_i)$ | 어느 위치에서 크게 벗어나는지 |
| 함수 RMSE | $\sqrt{n^{-1}\sum_i\delta_j(X_i)^2}$ | 전체적인 함수 근사 정확도 |
| 함수 MAE | $n^{-1}\sum_i|\delta_j(X_i)|$ | 평균 절대오차 |
| 오차 분포 | 절대오차 분위수·최댓값 | 평균이 가리는 큰 오차 확인 |
| 함수 비교 그림 | true 값과 fitted 값의 산점도, 위치별 오차 | 체계적 편향·국소 적합 문제 확인 |

Training residual RMSE가 작다는 이유만으로 true 함수를 잘 맞췄다고 결론 내리지 않음 - training residual에는 noise가 섞여 있어서 함수 근사 오차와 다른 양

현재 주 평가 대상은 고정 training 입력이며, 입력분포 전체에서의 근사 성능까지 확인하려면 별도 diagnostic 입력을 사용하는 보조 평가 가능

## 5.2 모델 확인 이후의 진행

- 구간 비교 전에 DGP별 함수 근사 결과를 먼저 보고
- 충분히 잘 맞춘다는 수치 기준은 아직 미정이며 임의의 통과 기준을 만들지 않음
- 정확도가 부족하면 해당 DGP의 모델 설정·적합 상태를 우선 검토하고 결과에 명시
- RF 튜닝과 함수 정확도 pilot은 본 실험 전에 진행 가능하며, 확정한 설정은 본 실험에 일관되게 적용
- 우연히 잘 맞은 seed나 반복만 골라 MC·CP 결과를 보고하지 않음
- True 함수를 튜닝 기준으로 활용한다면 시뮬레이션 전용 선택임을 기록
- 함수 근사가 좋더라도 분산 추정과 MC 오차분포 가정까지 맞는다는 뜻은 아니므로 다음 단계에서 별도 확인

## 5.3 잔차분산 추정

모델의 함수 근사 상태를 확인한 뒤 $r_i=Y_i^{train}-\widehat f_j(X_i)$ 계산 → Training residual $r_i$로 $\widehat\sigma_j^2$ 추정

$$
\widehat\sigma_j^2 = \frac1n\sum_i r_i^2
$$

- RF의 구체적인 분모·보정 규칙은 미정 (예: OOB residual 사용, 자유도 보정 등 — 향후 결정 필요)
- $\frac{1}{n}\sum_i r_i^2$는 선택 가능한 규칙이지만 확정된 불편추정량으로 취급하지 않음
- Training residual에는 noise와 적합 상태가 함께 반영되므로 true noise variance와 비교하여 진단 (9절의 $\widehat\sigma_j^2 - v_{design}$)
- 현재 분산 추정용 데이터는 training residual이며, 다른 데이터나 OOB 방식으로 변경할 때는 방법을 별도 명시
- 이 $\widehat\sigma_j^2$는 **DGP당 단 하나의 상수**이며, 이후 200번의 MC 반복 동안 전혀 안 바뀜 (0절 참고)

## 5.4 모델 확장

현재 구현은 Random Forest에 집중

추후 deep learning 등을 추가할 때도 **학습(1회) → true 함수 근사 확인 → 잔차분산 추정(1회) → 동일 모델의 MC·CP 비교(200회 반복)** 순서 적용 — “모델 재학습 없이 MC 반복”이라는 구조 자체는 모델이 바뀌어도 유지

모델마다 필요한 학습·분산 추정 설정은 명시하고, 구간 생성·coverage 평가·결과 저장 절차는 재사용

# 6. MC 예측구간 생성

## 6.1 입력 하나·반복 하나에서의 과정

Fitted model $\widehat f_j$와 $\widehat\sigma_j^2$ 고정 (이 절 전체에서 **재학습·재추정 없음**)

→ 입력 $X_i$에서 가상 반응값 $M_{MC}=1000$개 생성

$$
\epsilon_{i,r,s,j}^*\sim N(0,\widehat\sigma_j^2),\qquad
Y_{i,r,s,j}^*=\widehat f_j(X_i)+\epsilon_{i,r,s,j}^*
$$

→ 이 1,000개 반응값의 양쪽 empirical quantile로 MC 구간 구성

$$
C_{MC,j}^{(r)}(X_i)=
[\widehat Q_{\alpha/2}(Y_{i,r,1:M_{MC},j}^*),\ \widehat Q_{1-\alpha/2}(Y_{i,r,1:M_{MC},j}^*)]
$$

| 목표 coverage | 아래 분위수 | 위 분위수 |
| --- | --- | --- |
| 90% | 0.05 | 0.95 |
| 95% | 0.025 | 0.975 |
| 99% | 0.005 | 0.995 |

- 한 입력·한 반복에서 가상 반응값 1,000개로 구간 하나 구성
- 각 입력에서 난수를 새로 생성하여 200회 반복, 모델·추정분산은 고정 (재학습 없음 — 0절, 4절 13번 참고)
- 한 입력당 200,000개, 입력 1,000개 전체에서는 200,000,000개 가상 반응값 생성
- 같은 구간을 200번 복사하지 않고 매 반복 새 가상 표본으로 구간 구성
    
    이게 CP와 결정적으로 다른 점 (0.1절)
    
- 입력별로 분위수를 계산하고, 서로 다른 입력의 반응값들을 섞지 않음
- 같은 표본에서 세 coverage 수준을 함께 계산
- Empirical quantile의 보간 규칙을 설정과 결과에 기록
- 구현 기본안은 입력별 독립 MC 난수이며, 입력 간 공통 난수로 바꿀 경우 의존 구조를 명시

## 6.2 MC 수치 검증용 기준

고정된 fitted model과 추정분산에서 정규분포의 이론적 구간은 다음과 같음

$$
C_{normal,j}(x)=
[\widehat f_j(x)+\widehat\sigma_j z_{\alpha/2},\
 \widehat f_j(x)+\widehat\sigma_j z_{1-\alpha/2}]
$$

이 구간은 MC 분위수 근사의 정확도를 확인하는 **추정분포 기준값**

- 주 MC 구간은 생성한 표본의 empirical quantile로 계산
- MC 표본이 유한하므로 구간 midpoint가 $\widehat f_j(x)$와 정확히 같지 않을 수 있음
- MC 표본 수 증가로 줄어드는 것은 분위수 근사오차이며 모델·분산 추정오차는 그대로 남음
- $M_{MC}=1000$이 특히 99% 구간에 충분한지는 실제 pilot으로 확인 (99% 구간은 0.5%/99.5% 분위수를 쓰는데, 1000개 표본에서 극단 분위수 추정은 표본추출 변동이 상대적으로 크므로 우선순위 높은 점검 대상)

## 6.3 왜 MC가 CP보다 계산이 오래 걸리는가 (실행 전 예상)

CP는 4절 11번에서 보듯 **DGP당 1번**만 calibration residual quantile을 계산하면 끝나지만, MC는 200번의 반복마다 입력 1000개 전부에서 표본을 새로 뽑아야 함

- CP 계산량: calibration 1,000개 잔차 정렬 × (DGP당 1회) - 작음
- MC 계산량: $1000\text{(입력)} \times 200\text{(반복)} \times 1000\text{(MC표본)} = 2$억 개/DGP - 훨씬 큼

이 비대칭 때문에 “MC 파트가 시간이 오래 걸릴 것”이라는 예상(11절에서 전체 계산량과 대응 방안을 구체적으로 다룸)

## 6.4 Calibration 고정 정책의 근거

CP 구간을 200번 반복 동안 상수로 유지하는(calibration “전체 세트 고정”) 정책을 채택하는 이유:

- 연구 질문이 “동일한 fitted model을 사용할 때”라고 명시했고, 이는 모델뿐 아니라 그 모델에서 파생된 CP 분위수 $\widehat q_\alpha$도 하나로 고정하는 것이 자연스러운 해석
- MC도 $\widehat f_j$, $\widehat\sigma_j^2$를 고정하는데, CP만 calibration을 매번 새로 뽑으면 두 방법의 “고정 대상”이 서로 달라져서 비교가 덜 깔끔해짐
- 다만 이건 잠정 결론이며, “반복마다 새로 생성” 정책(각 CP 구간과 해당 true 반응값을 1:1 대응)이 왜 필요한지에 대한 반론이 있다면 재검토 가능 (8절 표 참고)

# 7. Split CP 예측구간 생성

Training 입력을 복사하지 않고 별도 calibration 입력 1,000개 생성

→ true model로 calibration 반응값 생성

→ MC와 동일한 fitted model로 absolute residual 계산

$$
R_{a,j}=|Y_a^{cal}-\widehat f_j(X_a^{cal})|
$$

→ 정렬 후 $k_\alpha=\lceil(n_{cal}+1)(1-\alpha)\rceil$번째 residual 선택

→ $\widehat q_{j,\alpha}=R_{(k_\alpha),j}$를 사용하여 구간 구성

$$
C_{CP,j}(X_i)=
[\widehat f_j(X_i)-\widehat q_{j,\alpha},\
 \widehat f_j(X_i)+\widehat q_{j,\alpha}]
$$

| $n_{cal}=1000$ | 90% | 95% | 99% |
| --- | --- | --- | --- |
| 순위 $k_\alpha$ | 901 | 951 | 991 |

$k_\alpha>n_{cal}$이면 분위수를 $+\infty$로 처리

**이 절의 모든 계산은 DGP당 1번만 수행됨.** $\widehat q_{j,\alpha}$가 확정되면 $C_{CP,j}(X_i)$는 모든 $X_i$에 대해 이미 정해지고, 이후 200번의 $r$ 반복 동안 이 값을 그대로 재사용(6.1절의 MC와 대비되는 지점, 0.1절 참고).

Calibration을 고정할지 바꿀지는 현재 “전체 세트 고정”으로 잠정 채택 (6.4절 근거). 위 과정은 주어진 calibration 한 세트에서의 계산.

> 별도 calibration을 사용한다는 사실만으로 현재 고정된 training 위치들의 coverage가 목표 수준을 만족한다고 단정할 수 없음
> 
> 
> 현재 평가는 학습에 사용한 입력 위치를 재방문하는 평가이므로, 일반적인 새 입력에 대한 CP marginal 보장을 그대로 주장하지 않음
> 

# 8. 200개 구간의 포함 여부로 coverage 평가

입력 $X_i$, fitted model, 추정분산을 고정하고 $r=1,\ldots,R_{MC}$ 반복, $R_{MC}=200$은 임시 설정

| 한 반복 $r$ | 과정 | 재계산 여부 |
| --- | --- | --- |
| 1 | 추정분포에서 $Y^*$ 1,000개 새로 생성 | MC만, 매번 새로 |
| 2 | 표본 분위수로 $C_{MC,j}^{(r)}(X_i)$ 구성 | MC만, 매번 새로 |
| 2’ | $C_{CP,j}(X_i)$는 7절에서 이미 계산된 상수를 그대로 사용 | 재계산 없음 |
| 3 | True DGP에서 독립적인 $Y_{i,r}^{test}$ 하나 새로 생성 | 매번 새로 |
| 4 | 두 구간이 해당 반응값을 포함하면 1, 아니면 0 기록 | — |

$$
Y_{i,r}^{test}=f(X_i)+\sigma_0(X_i)\varepsilon_{i,r}^{test},\qquad
\varepsilon_{i,r}^{test}\sim F_\varepsilon
$$

$$
I_{MC,j,i,r}=\mathbf1\{Y_{i,r}^{test}\in C_{MC,j}^{(r)}(X_i)\},\qquad
\widehat{cov}_{MC,j}(X_i)=\frac1{R_{MC}}\sum_{r=1}^{R_{MC}}I_{MC,j,i,r}
$$

**200개 구간과 200개 true 반응값을 1:1로 대응시켜, 200개의 0·1 합을 200으로 나누는 평가**

- True 반응값은 매 반복 새로 생성하고 MC 가상 표본과 독립
- 하나의 true 반응값을 200개 구간에 재사용하거나, 200×200개의 모든 조합을 평가하지 않음
- Training·calibration 반응값이나 $Y^*$를 true 평가값으로 재사용하지 않음
- 190회 포함이면 190/200=0.95, 비율의 간격은 1/200=0.005
- 200회는 임시 반복 수이며, 특히 99% 수준의 정밀도를 충분히 확보했다는 뜻은 아님
- 모델·추정분산은 이 200회 동안 고정이므로 coverage는 해당 fitted model·추정분산 조건에서 MC 구간 생성과 새 반응값의 변동을 평균한 값

## CP와의 대응

같은 $Y_{i,r}^{test}$로 CP의 포함 여부도 계산

$$
I_{CP,j,i,r}=\mathbf1\{Y_{i,r}^{test}\in C_{CP,j}^{(c(r))}(X_i)\},\qquad
\widehat{cov}_{CP,j}(X_i)=\frac1{R_{MC}}\sum_{r=1}^{R_{MC}}I_{CP,j,i,r}
$$

$c(r)$은 해당 반복에서 사용하는 calibration 세트 ID. **현재 정책(전체 세트 고정)에서는 $c(r)$이 $r$에 상관없이 항상 같은 ID이므로, $C_{CP,j}^{(c(r))}(X_i)$는 사실상 $r$에 의존하지 않는 상수 $C_{CP,j}(X_i)$가 된다.**

| Calibration 정책 | CP 구간 | 평가 | 채택 여부 |
| --- | --- | --- | --- |
| 전체 세트 고정 | 동일 모델·입력에서는 같은 CP 구간 | 같은 CP 구간에 서로 다른 true 반응값 200개 적용 | **잠정 채택** (6.4절) |
| 반복마다 새로 생성 | 각 반복에서 CP 구간 재계산 | 각 CP 구간과 해당 true 반응값을 1:1 대응 | 미채택 |

두 정책 중 어느 것을 사용할지는 최종 확정이 아니며, MC의 200회 반복을 정했다고 calibration 재생성까지 확정된 것은 아니라는 점을 계속 유의. 단, 이번 논의에서 “MC·CP가 같은 fitted model을 공유하므로 모델에 따른 차이는 없다”는 전제를 확인했고, 이는 “전체 세트 고정” 정책과 정합적이라는 근거가 6.4절에 추가됨.

구간당 포함 확률의 CDF 계산은 선택적 수치 검증에만 사용하고, 주 결과는 위 0·1의 평균으로 계산

# 9. 평가지표와 해석

| 지표 | 계산 | 역할 |
| --- | --- | --- |
| 입력별 coverage | 200회 중 포함 횟수 / 200 | 주 비교 |
| 목표와 차이 | $\widehat{cov}_{A,j}(X_i)-(1-\alpha)$ | 과소·과대 coverage 확인 |
| 방법 간 차이 | $\widehat{cov}_{CP,j}(X_i)-\widehat{cov}_{MC,j}(X_i)$ | 같은 위치에서 직접 비교 |
| 구간 길이 | 각 반복의 $U^{(r)}-L^{(r)}$, 200회 평균·SD (CP는 $r$과 무관하게 항상 동일한 길이 $2\widehat q_\alpha$) | Coverage와 함께 비교 |
| 구간 중심·반폭 | $(L+U)/2$, $(U-L)/2$ | 보조 진단 |
| 모델 오차 | $\delta_j(X_i)=\widehat f_j(X_i)-f(X_i)$ | True 함수 근사 상태 |
| 함수 RMSE | $\sqrt{n^{-1}\sum_i\delta_j(X_i)^2}$ | 구간 비교 전 첫 단계의 정확도 확인 |
| 함수 MAE | $n^{-1}\sum_i|\delta_j(X_i)|$ | 보조 정확도 |
| 분산 진단 | $\widehat\sigma_j^2$, $v_{design}=n^{-1}\sum_i\sigma_0^2(X_i)$, $\widehat\sigma_j^2-v_{design}$ | Training 입력에서의 평균 true noise variance와 비교 |
| 위치별 true 분산 | $\sigma_0^2(X_i)$ | 이분산 패턴 확인 |

이분산 조건의 $\widehat\sigma_j^2$는 하나의 상수이므로 위치별 true 분산 전체에 대한 추정치로 해석하지 않음 — 이게 바로 2.5절에서 예상한 “hetero 조건에서 MC coverage가 위치별로 벌어지는” 원인

고정 입력 전체 coverage 요약은 $n^{-1}\sum_i\widehat{cov}_{A,j}(X_i)$

- 이름: **고정 입력 평균 coverage**
- 임의의 새로운 입력분포에 대한 marginal coverage로 자동 해석하지 않음
- 함수 RMSE는 true 함수와의 차이이며, 관측 training $Y$에 대한 residual RMSE와 구분
- 같은 fitted model을 사용해도 두 방법의 coverage는 다를 수 있음 — 이게 연구의 핵심 결과가 됨
- 모델 정확도는 MC·CP 비교에 앞서 확인하는 첫 단계이며, 최종 결과에도 함께 보고
- 짧은 구간이더라도 coverage가 부족하면 더 좋다고 단정하지 않음

## 9.1 5×5 grid 시각화 — 1000개 주 평가의 보조 시각화

**주 평가는 여전히 1000개 training 위치다.** 연구의 핵심 결과(coverage, 방법 간 차이, “고정 입력 평균 coverage” 등, 9절)는 전부 1000개 위치 기준으로 계산하고 보고한다. 1000개는 계산량이 크지만(11.2절, DGP당 2억 개), 위치별 coverage 패턴을 세밀하게 잡아내려면 필요한 규모이므로 축소하지 않는다.

5×5=25개 grid는 이 1000개 결과를 **대체하는 것이 아니라, 눈으로 보기 좋게 요약해서 보여주는 보조 수단(**1000개 산점도는 패턴을 읽기 어려우므로, grid point 25개에서 별도로 같은 절차를 수행해 heatmap으로 표현)

**Grid 좌표**: $x_1, x_2 \in \{-0.8,\ -0.4,\ 0,\ 0.4,\ 0.8\}$ ($[-1,1]$ 균등 5등분), 총 $5\times5=25$개 지점 $X_i^{grid} = (g_1, g_2)$

**핵심: 근방 training 위치들의 결과를 평균 내는 방식(binning)은 채택하지 않는다.** 이유:

- Coverage는 원래 “그 입력 $x$에서” 정의되는 조건부 개념이다 (8절의 $\widehat{cov}_{MC,j}(X_i)$는 특정 $X_i$ 하나에 대한 값). 근방 점들을 평균 내면 “그 칸 근방의 평균 coverage”라는 다른 양이 되어 정의가 흐려진다
- Training 위치가 랜덤 생성이라 칸마다 들어가는 점 개수가 불균등하고, 표본이 적은 칸은 노이즈가 커진다
- 반면 grid point에서 직접 절차를 수행하면 이런 문제가 아예 없고, 해석도 “이 grid point의 coverage는 얼마다”로 명확하다

**따라서 25개 grid point 각각에서 4절 13~16번(MC 반복·CP 평가)을 그대로 수행한다:**

```
[DGP당, 모델 학습·분산추정·CP분위수 계산은 이미 완료된 것을 그대로 재사용 — 재학습 없음]

for grid point X_i^grid in 25개 지점:
    f_hat_j(X_i^grid) 계산 (이미 학습된 모델로 예측만 수행)

    for r = 1, ..., 200:
        # MC
        eps*_s ~ N(0, sigma_hat_j^2),  s=1,...,1000
        Y*_s = f_hat_j(X_i^grid) + eps*_s
        C_MC^(r)(X_i^grid) = quantile(Y*_1:1000, [alpha/2, 1-alpha/2])

        # CP: 이미 계산된 q_hat_alpha를 그대로 적용, 재계산 없음
        C_CP(X_i^grid) = f_hat_j(X_i^grid) ± q_hat_alpha

        # true 평가값 — grid point에서 새로 생성
        Y_test = f(X_i^grid) + sigma0(X_i^grid) * eps_test

        I_MC[i,r] = 1{Y_test in C_MC^(r)}
        I_CP[i,r] = 1{Y_test in C_CP}

    cov_MC(X_i^grid) = mean_r I_MC[i,r]
    cov_CP(X_i^grid) = mean_r I_CP[i,r]
```

- 모델 재학습이나 분산·CP분위수 재추정은 전혀 없음 — $\hat f_j$, $\hat\sigma_j^2$, $\hat q_\alpha$는 training/calibration에서 이미 확정된 값을 그대로 사용하고, grid point에서는 그 값들을 **적용(evaluate)만** 함
- 계산량은 1000개 training 위치 대신 25개 grid에서만 도는 것이므로, 기존 절차 대비 훨씬 가벼움 ($25 \times 200 \times 1000 = 500$만 개/DGP 수준, 1000개 대비 약 1/40). 이 계산량 차이 덕분에 grid 시각화를 추가해도 전체 실행 시간에 큰 부담이 없음 — 반대로 이 그리드 25개를 주 평가로 승격시키지 않는 이유이기도 함(11절에서 논의한 “1000개가 오래 걸리면 25개로 축소” 대안과는 별개로, 지금은 1000개를 주 평가로 유지하는 결정)
- 함수 근사 오차 $\delta_j(X_i^grid) = \hat f_j(X_i^grid) - f(X_i^grid)$도 같은 방식으로 grid point에서 직접 계산

**출력 형태**: DGP(12개) × α(3개) × 방법(MC/CP) 조합마다 5×5 heatmap 1장. 주요 사용처:

- coverage heatmap: 어느 $(x_1,x_2)$ 지점에서 coverage가 목표치보다 부족/과다한지
- coverage 차이(CP−MC) heatmap: 두 방법의 격차가 큰 지점 확인 — 특히 hetero 조건에서 $\sigma_0(x)$가 $x_1$에 의존하므로($\{0.4+1.2\sin^2(\pi x_1)\}/\sqrt{1.18}$), $x_1$ 방향으로 패턴이 뚜렷하게 나타날 것으로 예상
- 함수 오차($\delta_j$) heatmap: 모델이 어느 지점에서 근사를 잘 못하는지

# 10. 반복 구조 요약

| 반복 단위 | 고정·변화 대상 | 횟수 | DGP당 실행 위치 |
| --- | --- | --- | --- |
| 모델 학습 | training set 1회 사용 | **1회** | 4절 3번 |
| 잔차분산 추정 | training residual $r_i$ 1회 사용 | **1회** | 4절 7번 |
| CP 분위수 계산 | calibration set 1회 사용 | **1회** | 4절 11번 |
| MC 내부 표본 | 모델·추정분산·입력 고정, 가상 오차 생성 | $M_{MC}=1000$ (한 구간 구성당) | 4절 13번 |
| MC 구간·평가 반복 | 모델·추정분산·입력 고정, 구간과 true 반응값 새로 생성 | $R_{MC}=200$, 임시 | 4절 13~16번 |
| 전체 실험 | 새 training 반응값으로 학습부터 반복할지와 횟수 | 정책·$B$ 미정, 우선 $B=1$ | (미도입) |
| Calibration | 200회 동안 전체 세트를 유지할지 반복마다 새로 생성할지 | **정책: 고정** (잠정) | 6.4절 |

Calibration 고정은 입력만 고정하고 반응값을 바꾸는 것과, 입력·반응값을 모두 고정하는 것이 다르므로 추후 명시 — 현재 잠정 정책은 입력·반응값 모두 고정(즉 calibration set 자체를 통째로 고정)

전체 실험에서 모델을 바꾸면 같은 calibration 데이터를 유지하더라도 residual과 CP 분위수는 새 모델로 재계산

실험 반복을 도입하면 반복별 결과와 평균을 구분하고, 독립 반복이 확보된 경우에만 반복 간 표준오차를 해당 단위로 계산

# 11. 결과 저장과 계산량 — MC 파트가 오래 걸리는 이유와 대응

## 11.1 필수 저장물

1. 최종 설정·입력행렬·난수 seed·모델 설정
2. 입력별 true mean·fitted mean·함수 추정오차
3. 모델별 training residual $r_i$ 요약·분산 추정값·추정 규칙
4. 입력·반복별 MC·CP endpoint·true 반응값·포함 여부·길이
5. 200회 포함 횟수·coverage·평균 길이와 CP−MC 차이
6. 고정 입력 평균 결과와 실제 반복 단위
7. 수치 검증 결과·단계별 실행시간 (특히 MC 파트 소요시간을 별도로 기록 — 계산량이 CP보다 훨씬 크므로)

같은 입력의 예측값 $\widehat f_j(X_i)$는 한 번 계산하여 재사용하고, MC 반응값과 평가 반응값은 입력 단위 또는 chunk 단위로 처리 가능

## 11.2 계산량 — 숫자로 확인

한 DGP·한 fitted model에서:

- MC 가상 반응값: $1000\text{(입력)}\times200\text{(반복)}\times1000\text{(MC표본)}=2$억 개
- True 평가 반응값: $1000\times200=20$만 개
- CP 관련: calibration 1,000개 잔차 — MC 대비 무시할 수준

12개 DGP 전체에서는 MC 가상 반응값 총 **24억 개**이며, 세 coverage 수준(90/95/99%)은 같은 표본에서 동시에 계산하므로 생성량을 3배로 늘리지 않음.

이 비대칭(MC 24억 개 vs CP 12,000개)이 “MC 파트가 시간이 엄청 오래 걸릴 것”이라는 예상의 근거다. CP는 DGP당 1번 계산으로 끝나는 반면, MC는 200번 반복 × 1000개 입력마다 1000개씩 새로 뽑아야 하기 때문.

## 11.3 메모리·속도 대응 방안

전체 24억 개를 한꺼번에 메모리에 올리지 않는다. 권장 처리 방식:

- 반복 $r$ 또는 입력 $X_i$ 단위(혹은 둘 다 chunk)로 나눠서 생성·분위수 계산 후 즉시 폐기
- 예: numpy에서 $(n\_chunk, 1000)$ 크기 배열만 유지 → `np.quantile(..., axis=1)`로 분위수 계산 → 지시함수만 기록하고 원본 표본은 버림
- 입력 1000개를 한 번에 벡터화(vectorize)해서 $(1000, 1000)$ 배열로 만들고, $r$에 대해서만 200번 파이썬 루프를 도는 방식이 현실적 (입력 축은 벡터 연산, 반복 축만 루프)
- 병렬화 여지: DGP 12개는 서로 완전히 독립이므로 DGP 단위로 병렬 처리 가능 (멀티프로세싱)

실제 소요시간은 모델·전체 반복·평가 표본 수·환경에 따라 달라지므로 실행 전 장시간 또는 단시간이라고 단정하지 않음. 다만 pilot(작은 규모, 예: $n=50$, $R_{MC}=10$)으로 먼저 파이프라인을 검증하고 소요시간을 추정한 뒤, 본 규모로 확장하는 것을 권장.

표를 먼저 생성하고, 필요 시 고정 입력의 위치별 산점도·heatmap을 후속 작성

# 12. 구현 전 남은 결정

| 결정 | 필요한 내용 | 현재 상태 |
| --- | --- | --- |
| DGP 실행 | 12개 조건의 scenario ID·실행 순서·seed | 미정 |
| 입력 | 고정 training 입력의 seed와 반복 간 공유 범위 | 미정 |
| 분산 추정 | Random Forest의 training residual $r_i$ 처리 규칙 (분모·보정) | 미정, 우선 $n^{-1}\sum r_i^2$ |
| 모델 | RF 하이퍼파라미터·튜닝 절차·함수 근사 정확도 판단 기준 | 미정 |
| 평가 | 임시 $R_{MC}=200$의 정밀도 확인과 최종 반복 수 | 미정, pilot 필요 |
| 반복 | $B$, training 입력 유지 범위, calibration 고정·재생성 정책 | calibration은 “고정”으로 잠정 채택(6.4절), $B$는 미정 |
| 계산 최적화 | MC 파트 chunk 처리·병렬화 방식 | 11.3절 방향만 제시, 구체 구현 미정 |
| 주 평가 규모 | 1000개 vs 25개(5×5 grid) — pilot에서 1000개가 감당 불가능할 정도로 오래 걸리면 재검토 | **1000개를 주 평가로 유지, 25개 grid는 보조 시각화로만 사용** (9.1절 확정) |

설계 상태: 데이터 생성·구간 구성·평가 절차 정의 완료, 모델 학습 횟수(DGP당 1회) 및 MC·CP 구간의 재계산 시점 차이를 명확히 함. 세부 설정(분산 추정 규칙, seed, RF 하이퍼파라미터, 반복 수 최종 확정) 결정 및 실제 실행 전.